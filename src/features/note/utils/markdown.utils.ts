/**
 * @deprecated This file has been split into focused sub-modules.
 * Import directly from:
 *   - markdown.conversion  (escapeRegex, convertToDisplayVersion, convertToOriginalVersion,
 *                           extractExternalLinks, extractHeadingsAsKeywords)
 *   - markdown.decorations (updateDecorations)
 *   - markdown.autocomplete (setupAutocomplete)
 *   - markdown.providers   (setupDefinitionProvider, setupMarkdownFolding, setupHoverProvider)
 *   - markdown.link        (setupLinkProvider)
 */
