/**
 * Note Feature Constants
 */

/** Entity type ID for notes in workspace tree */
export const NOTE_ENTITY_TYPE = 3;

/** Delay before focusing the note name input after creating a new note */
export const NOTE_FOCUS_DELAY_MS = 100;

/** Debounce delay for saving scroll position while the user scrolls */
export const SCROLL_SAVE_DEBOUNCE_MS = 100;

/** Delay before restoring editor scroll position after a tab switch */
export const EDITOR_RESTORE_DELAY_MS = 50;

/** Minimum container width (px) to show the workspace-links column */
export const NOTE_COL_WORKSPACE_MIN_WIDTH = 462;

/** Minimum container width (px) to show the status column */
export const NOTE_COL_STATUS_MIN_WIDTH = 572;

/** Minimum container width (px) to show the created-date column */
export const NOTE_COL_CREATED_DATE_MIN_WIDTH = 682;

/** Minimum container width (px) to show the deleted column */
export const NOTE_COL_DELETED_MIN_WIDTH = 742;
