/**
 * SensitiveOverlay - placeholder, blur disabled (to be implemented later)
 */

interface SensitiveOverlayProps {
    children: React.ReactNode;
}

export function SensitiveOverlay({ children }: SensitiveOverlayProps) {
    return <>{children}</>;
}
